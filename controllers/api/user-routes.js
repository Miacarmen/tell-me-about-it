const router = require('express').Router();

const { user } = require('../../models');


// POST signup
// prompt to sign up
// then user credentials are saved

// POST login
// prompt to sign in

// when they revist a site and click login
// then prompt to enter username and password

// POST logout
// destroy session

module.exports = router;